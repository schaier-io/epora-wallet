.DS_Store
**/.DS_Store
# local AI-tooling config — never publish
**/.claude/
# internal QA/dogfood notes — local only, never publish
code/dApp/.audit/
designs/logo/logo.afdesign~lock~
.pnpm-store/

# dependencies — ignored at every level (each package also ignores its own)
node_modules/

code/dApp/.env
code/smart-contract/.env
code/smart-contract/*.sk

# Environment files may contain deployment secrets. Keep only public templates.
**/.env*
!**/.env.example
