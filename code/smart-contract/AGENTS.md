# Smart-contract rules (Aiken/Plutus)

The contract engineering rules live in [CLAUDE.md](CLAUDE.md) in this
directory. Read that file; it is the single source of truth. This stub exists
only so tools that auto-discover nested AGENTS.md files find the pointer.
