# Aiken compilation artifacts
artifacts/
# Aiken's project working directory
build/
# Aiken's default documentation export. The former docs/ allow-list (ADRs,
# security-audit / threat-model / hardening-backlog) was dropped with those
# files in the 2026-06 slimming pass — the whitepaper is the decision record
# now (CLAUDE.md §7), so nothing under docs/ is tracked any more.
docs/**
# `pnpm build:debug` blueprint — traces compiled in, never deployed
plutus-debug.json

node_modules/
.env
# Generated key material and derived addresses (per-developer; never commit)
*.sk
*.addr
